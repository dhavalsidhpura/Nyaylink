// Compatibility exports for older imports. Keep service data in the canonical module.
export { ALL_CATEGORIES, MASTER_SERVICES, default } from '@/data/services';
export type { ServiceItem } from '@/data/services';
